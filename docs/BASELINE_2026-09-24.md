# 运行时可靠性收口 · 基线锚点（2026-09-24）

> **用途**：本轮《运行时可靠性收口》（`P3_MEMORY_WORKLOG.md` 第二十节）的**回退锚点**与
> **改动前事实快照**。开工前建立，此后**只读**；所有"改前 / 改后"对照以本文件为基准。
>
> **范围**：按设计稿 20.5，本阶段（步 0）**无行为变化**——本文件 + 同名 tag 就是第 0 步的全部交付。

## 0. 锚点

| 项 | 值 |
|---|---|
| 分支 / commit | `main` @ `4b97e03`（`docs: 记录《运行时可靠性收口》三项评审裁决（D1/D2/D3）`） |
| 回退 tag | `p3-before-runtime-hardening` |
| 回退方式 | `git checkout p3-before-runtime-hardening`（单人开发，不动分支拓扑，见 20.5 对 GPT 建议的改写） |

## 1. 环境

- Python **3.12.10**（`elysia/.venv`；`uv` **editable** 安装：`_editable_impl_elysia.pth` → `src`，
  因此"导入即工作区源码"，无需重装即可生效）
- 平台：Windows；工作目录 `a:\WorkPlace\Elysia\elysia`
- 门禁工具链（`pre-commit`）：ruff-lint ／ ruff-format ／ **mypy（`strict = true`）** ／ pytest；
  `line-length = 100`、`target-version = "py312"`（`pyproject.toml`）

## 2. 规模基线

| 项 | 值 |
|---|---|
| `src` | **52 文件 / 7956 行** |
| `tests` | **33 文件 / 7077 行 / 375 项**（acceptance 30 项 + unit 345 项） |
| 最大文件 | `pet.py` 695 ／ `heartbeat.py` 616 ／ `state_store.py` 601 ／ `expression_service.py` 593 ／ `retrieve.py` 573 ／ `memory_view.py` 539 |
| 完整性清单 | `工具脚本/scripts/verify_integrity.py`，当时 **130 文件**（本文件加入后为 131） |

## 3. 状态库 schema

**无显式版本号常量**——schema = `core/state_store.py` 的建表语句 + **幂等** `ALTER` 迁移（跑 N 次 = 跑 1 次）。

| 库 | 表 |
|---|---|
| `data/state.db` | `kv` |
| `data/heartbeat.db` | `heartbeats` ／ `thought_log` ／ `expression_log` ／ `memories` ／ `memory_index` |

`HeartbeatStore` 迁移 **5 条**（补列 `superseded_by` ／ `source` ／ `certainty` ／ `claim_status`
／ `retention_state`）+ 回填 **4 条**（`source`、`certainty` 按 `kind` 推导；`claim_status`、
`retention_state` 与 `kind` 无关、一律默认值）。

**基线实测（只读，2026-09-24）**：`data/heartbeat.db` **52.3 MB**（WAL 伴生文件另计）、
`heartbeats` **120,871 行**、`memories` **401 行**。

## 4. 启动 / 停止 / 观测

| 动作 | 命令 |
|---|---|
| 启动（灵魂 + 身体） | `powershell -ExecutionPolicy Bypass -File scripts\soul.ps1 start -Body` |
| 停止 | `powershell -ExecutionPolicy Bypass -File scripts\soul.ps1 stop` |
| 状态 | `powershell -ExecutionPolicy Bypass -File scripts\soul.ps1 status` |
| TTS 服务（出声所需） | `声音模型\GPT-SoVITS-v2pro-20250604\启动ElysiaAPI.bat`（端口 **9880**；未启动 ⇒ 她只出文本、不出声） |
| 记忆浏览器 | `powershell -ExecutionPolicy Bypass -File scripts\memory_view.ps1`（**独立进程**，须重开才能看到新增列） |

## 5. 已知问题（**不重写，直接引用**）

- **缺点 8 条（按严重度，含证据 / 位置 / 风险）**：`P3_MEMORY_WORKLOG.md` **19.4**
- **与 GPT 规划的差距对照**：同文件 **19.7**
- **解决方案三档（A 立刻 / B 近期 / C 观察）**：同文件 **19.5**

本轮（第二十节）范围与该清单的对应关系：只覆盖 **19.5 的 A 档第 2 项**
（`VramBreaker` 采样挪线程，即 20.6 的 1.5 附带小改），以及 19.4 缺点 1 中
"进程无自愈"里**可被程序自身解决的那一半**——**心跳不再等外部服务**。

## 6. 本轮唯一行为变化点

见同文件 **20.10**：**步 1.3（心跳接线）**才是她「不等了」的那一步；
**0.1 / 0.2 / 1.1 / 1.2 均零行为变化**（本文件即步 0.1 的交付）。